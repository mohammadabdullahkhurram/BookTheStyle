<div class="mb-7 w-full">
    <div class="text-[12.5px] font-semibold uppercase tracking-[0.04em] text-faint">{{ __('Account') }}</div>
    <h1 class="mt-1.5 font-display text-[26px] font-bold leading-none text-ink">{{ __('Settings') }}</h1>
    <p class="mt-2 text-[15px] text-secondary">{{ __('Manage your profile and account settings') }}</p>
</div>
