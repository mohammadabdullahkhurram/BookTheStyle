{{-- Shared "Business details / Address / Owner details" sections for the
     salon create screen and salon settings (the agency EDIT screen tabs the
     two halves separately — same sub-partials, different layout). --}}
@include('partials.salon-business-fields')
@include('partials.salon-owner-fields')
